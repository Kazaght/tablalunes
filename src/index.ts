let llamarfuncion: string = String (prompt("ingrese A, B C, o D"))
  function tabladel(numerodetabla: number) {
    for (let contador = 1; contador <= 9; contador++) {
      const resultado = contador * numerodetabla;
      console.log(resultado);
    }
  while (llamarfuncion === a ||
    llamarfuncion === b ||
    llamarfuncion === c ||
    llamarfuncion === d )
    tabladel(9) ; {
  
  tabladel(9);
  console.log(tabladel);
} else {
  console.log("Ingrese a,b,c o d");
}